baseURL = 'https://david.krentzlin.me'
languageCode = 'en-us'
title = 'David Krentzlin'
theme = "hugo-bearcub"
copyright = "It's almost never about how you start, but how you finish"

enableRobotsTXT = true

[taxonomies]
  context = 'contexts'
  kind = 'kinds'
  tag = 'tags'

[languages]
  [languages.en]
    title = "David Krentzlin"
    languageName = "en-US 🇺🇸"
    LanguageCode = "en-US"
    contentDir = "content"
    [languages.en.params]
      madeWith = "Finish strong!"

[params]
  description = "It's code, all the way down"
  favicon = "images/favicon.png"

  # These images will show up when services want to generate a preview of a link
  # to your site. Ignored if `generateSocialCard = true`. For more information
  # about previews, see https://gohugo.io/templates/internal#twitter-cards and
  # https://gohugo.io/templates/internal#open-graph
  images = ["images/share.webp"]

  # This title is used as the site_name on the Hugo's internal opengraph
  # structured data template
  title = "David Krentzlin"

  # Dates are displayed following the format below. For more information about
  # formatting, see https://gohugo.io/functions/format/
  dateFormat = "2006-01-02"

  # If your blog is multilingual but you haven't translated a page, this theme
  # will create a disabled link. By setting `hideUntranslated` to true, you can
  # have the theme simply not show any link
  hideUntranslated = true

  themeStyle = "original"

  generateSocialCard = false

  # Social media. Delete any item you aren't using to make sure it won't show up
  # in your website's metadata.
  [params.social]
    twitter = "certainty" # Twitter handle (without '@')

  # Author metadata. This is mostly used for the RSS feed of your site, but the
  # email is also added to the footer of each post. You can hide the "reply to"
  # link by using a `hideReply` param in front matter.
  [params.author]
    name = "David Krentzlin" # Your name as shown in the RSS feed metadata
    email = "david.krentzlin@gmail.com" # Added to the footer so readers can reply to posts